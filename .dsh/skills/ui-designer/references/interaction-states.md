# Interaction states

For interactive controls evaluate: default, hover (when relevant), keyboard focus, active/pressed, disabled, loading, error, success, and empty state. Preserve expected platform semantics and do not rely on color alone for state. Ensure focus remains visible and interaction feedback is timely and unambiguous.
